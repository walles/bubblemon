## Process this file with automake to produce Makefile.in

INCLUDES = \
	$(GNOME_INCLUDEDIR)

bin_PROGRAMS = bubblemon_applet

bubblemon_applet_SOURCES = \
	bubblemon.c bubblemon.h \
	session.c session.h

bubblemon_appletLDADD = $(GNOME_LIBDIR) $(GNOMEUI_LIBS)
